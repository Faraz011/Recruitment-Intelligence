export const metadata = {
  title: "Dashboard | JD Structura",
  description:
    "View and manage extracted job listings and interview scorecards",
};

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
